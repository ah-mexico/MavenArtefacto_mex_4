name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3      #usuarios del contenerdor
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: dir 1
      run: dir
    
    - name: cd
      run: | 
       cd CommonAvicenaAtencionExceptions
       mvn clean install
       cd target
       dir
       
    
    - name: dir 2
      run: dir     
